这个推荐算法和以往的有一些不同，过去都是给用户（买家）推荐商品，这次是给卖家推荐买家。
我采用了基于用户的协同过滤算法，根据用户过去半年的行为，求出用户之间的相似度，根据相似度矩阵，把与已经购买该商品用户相似的用户，推荐给卖家，
对其进行针对性的营销活动，从而提高广告效益。
预测方法采用准确度。
计算准确度的指标为，先计算每个推荐列表中用户收藏\购买的产品和所有被推荐产品的比率，然后把全部用户的比率求平均，得到准确度。
模型：基于用户的协同过滤
数据：用户过去半年的浏览、收藏、推荐和购买数据
训练：以分布式的方式在多台机器上并行训练
预测：根据准确度作为评判指标，来评价推荐算法。
